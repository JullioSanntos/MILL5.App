<?xml version="1.0" encoding="utf-8" ?>
<!-- auto-generated by the ViewsProject template -->
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             xmlns:models="clr-namespace:MILL09.Models;assembly=MILL09.Models"
             x:Class="MILL03.Views.Views.DepartmentsView">
    <Grid RowDefinitions="Auto, *">
        <Button Grid.Row="0" Text="Load Departments"
                Command="{Binding LoadDepartmentsCommand}"
                Margin="10" TextColor="White" BackgroundColor="#2B579A">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="PointerOver">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#3E6DB3" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#1C3F75" />
                                <Setter Property="Scale" Value="0.98" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
        </Button>

        <syncfusion:SfDataGrid Grid.Row="1" GridLinesVisibility="Both" HeaderGridLinesVisibility="Both"
                AutoGeneratingColumn="SfDataGrid_OnAutoGeneratingColumn" ColumnWidthMode="Fill" RowHeight="30"
                ItemsSource="{Binding Departments, Source={x:Static models:MainModel.Instance}}" />
    </Grid>
</ContentView>
