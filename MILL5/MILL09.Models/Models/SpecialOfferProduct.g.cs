// <auto-generated>
//     Generated from SpecialOfferProduct.cs by the ModelsProject template.
//     Regenerated whenever the source file changes.
// </auto-generated>

#nullable enable

#pragma warning disable IDE0130 // Namespace does not match folder structure

using System;
using System.Linq;
using System.Collections.ObjectModel;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.DependencyInjection;
using CommunityToolkit.Mvvm.ComponentModel;
using MILL09.Models.Interfaces;
using MILL80.Infrastructure.Services;
using MILL80.Infrastructure;

namespace MILL09.Models
{
public partial class SpecialOfferProduct : ModelEntityBase
{
    [ObservableProperty]
    private int _specialOfferId;

    [ObservableProperty]
    private int _productId;

    [ObservableProperty]
    private Guid _rowguid;

    [ObservableProperty]
    private DateTime _modifiedDate;

    public Product? Product => MainModel.Instance.Products.FirstOrDefault(x => x.ProductId == this.ProductId);

    public SpecialOffer? SpecialOffer => MainModel.Instance.SpecialOffers.FirstOrDefault(x => x.SpecialOfferId == this.SpecialOfferId);

    private ObservableCollection<SalesOrderDetail>? _salesOrderDetails;
    public ObservableCollection<SalesOrderDetail> SalesOrderDetails
    {
        // TODO: could not auto-detect the foreign key linking SalesOrderDetail back to SpecialOfferProduct.
        // Example: new LiveFilteredCollection<SalesOrderDetail>(MainModel.Instance.SalesOrderDetails, x => /* match condition */, nameof(SalesOrderDetail.SomeProperty))
        get { if (_salesOrderDetails == null) SalesOrderDetails = new ObservableCollection<SalesOrderDetail>(); return _salesOrderDetails!; }
        private set => SetProperty(ref _salesOrderDetails, value);
    }

    public static bool IsSpecialOfferProductsLoaded => MainModel.Instance.IsSpecialOfferProductsLoaded;

    public static Task LoadAllAsync(CancellationToken ct = default) =>
        IsSpecialOfferProductsLoaded ? Task.CompletedTask : RefreshAllAsync(ct);

    public static async Task RefreshAllAsync(CancellationToken ct = default)
    {
        OnRefreshing();
        var loader = MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySetLoader<SpecialOfferProduct>>();
        var fresh = await loader.LoadAllAsync(ct);
        var freshIds = fresh.Select(x => x.SpecialOfferId).ToHashSet();
        var collection = MainModel.Instance.SpecialOfferProducts;
        foreach (var freshItem in fresh)
        {
            var existing = collection.FirstOrDefault(x => Equals(x.SpecialOfferId, freshItem.SpecialOfferId));
            if (existing != null) existing.CopyScalarsFrom(freshItem);
            else collection.Add(freshItem);
        }
        for (int i = collection.Count - 1; i >= 0; i--)
            if (!freshIds.Contains(collection[i].SpecialOfferId)) collection.RemoveAt(i);

        // Explicitly flag the collection as loaded after a successful database query
        MainModel.Instance.IsSpecialOfferProductsLoaded = true;
        OnRefreshed();
    }

    public static void UnloadAll()
    {
        OnUnloading();
        MainModel.Instance.UnloadSpecialOfferProductsStorage();
        OnUnloaded();
    }

    static partial void OnRefreshing();
    static partial void OnRefreshed();
    static partial void OnUnloading();
    static partial void OnUnloaded();

    internal void CopyScalarsFrom(SpecialOfferProduct fresh)
    {
        SpecialOfferId = fresh.SpecialOfferId;
        ProductId = fresh.ProductId;
        Rowguid = fresh.Rowguid;
        ModifiedDate = fresh.ModifiedDate;
    }

    protected override Task OnSaveAsync(CancellationToken ct) =>
        MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySaver<SpecialOfferProduct>>().SaveAsync(this, ct);
}
}

namespace MILL09.Models
{
public partial class MainModel
{
    private ObservableCollection<SpecialOfferProduct>? _specialOfferProducts;
    public ObservableCollection<SpecialOfferProduct> SpecialOfferProducts
    {
        get { if (_specialOfferProducts == null) SpecialOfferProducts = new ObservableCollection<SpecialOfferProduct>(); return _specialOfferProducts!; }
        private set => SetProperty(ref _specialOfferProducts, value);
    }

    private bool _isSpecialOfferProductsLoaded;
    public bool IsSpecialOfferProductsLoaded
    {
        get => _isSpecialOfferProductsLoaded;
        internal set => SetProperty(ref _isSpecialOfferProductsLoaded, value);
    }

    internal void UnloadSpecialOfferProductsStorage()
    {
        if (_specialOfferProducts == null) return;
        _specialOfferProducts = null;
        OnPropertyChanged(nameof(SpecialOfferProducts));

        // Reset the load state flag
        IsSpecialOfferProductsLoaded = false;
    }
}
}
