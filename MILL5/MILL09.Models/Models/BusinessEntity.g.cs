// <auto-generated>
//     Generated from BusinessEntity.cs by the ModelsProject template.
//     Regenerated whenever the source file changes.
// </auto-generated>

#nullable enable

#pragma warning disable IDE0130 // Namespace does not match folder structure

using System;
using System.Linq;
using System.Collections.ObjectModel;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.DependencyInjection;
using CommunityToolkit.Mvvm.ComponentModel;
using MILL09.Models.Interfaces;
using MILL80.Infrastructure.Services;
using MILL80.Infrastructure;

namespace MILL09.Models
{
public partial class BusinessEntity : ModelEntityBase
{
    [ObservableProperty]
    private int _businessEntityId;

    [ObservableProperty]
    private Guid _rowguid;

    [ObservableProperty]
    private DateTime _modifiedDate;

    // TODO: could not auto-detect the foreign key for 'Person' -> Person.
    // Add a manual implementation in a separate partial file, e.g.:
    // public Person? Person => MainModel.Instance.Persons.FirstOrDefault(x => /* match condition */);

    // TODO: could not auto-detect the foreign key for 'Store' -> Store.
    // Add a manual implementation in a separate partial file, e.g.:
    // public Store? Store => MainModel.Instance.Stores.FirstOrDefault(x => /* match condition */);

    // TODO: could not auto-detect the foreign key for 'Vendor' -> Vendor.
    // Add a manual implementation in a separate partial file, e.g.:
    // public Vendor? Vendor => MainModel.Instance.Vendors.FirstOrDefault(x => /* match condition */);

    private ObservableCollection<BusinessEntityAddress>? _businessEntityAddresses;
    public ObservableCollection<BusinessEntityAddress> BusinessEntityAddresses
    {
        get { if (_businessEntityAddresses == null) BusinessEntityAddresses = new LiveFilteredCollection<BusinessEntityAddress>(MainModel.Instance.BusinessEntityAddresses, x => x.BusinessEntityId == this.BusinessEntityId, nameof(BusinessEntityAddress.BusinessEntityId)); return _businessEntityAddresses!; }
        private set => SetProperty(ref _businessEntityAddresses, value);
    }

    private ObservableCollection<BusinessEntityContact>? _businessEntityContacts;
    public ObservableCollection<BusinessEntityContact> BusinessEntityContacts
    {
        get { if (_businessEntityContacts == null) BusinessEntityContacts = new LiveFilteredCollection<BusinessEntityContact>(MainModel.Instance.BusinessEntityContacts, x => x.BusinessEntityId == this.BusinessEntityId, nameof(BusinessEntityContact.BusinessEntityId)); return _businessEntityContacts!; }
        private set => SetProperty(ref _businessEntityContacts, value);
    }

    public static bool IsBusinessEntitiesLoaded => MainModel.Instance.IsBusinessEntitiesLoaded;

    public static Task LoadAllAsync(CancellationToken ct = default) =>
        IsBusinessEntitiesLoaded ? Task.CompletedTask : RefreshAllAsync(ct);

    public static async Task RefreshAllAsync(CancellationToken ct = default)
    {
        OnRefreshing();
        var loader = MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySetLoader<BusinessEntity>>();
        var fresh = await loader.LoadAllAsync(ct);
        var freshIds = fresh.Select(x => x.BusinessEntityId).ToHashSet();
        var collection = MainModel.Instance.BusinessEntities;
        foreach (var freshItem in fresh)
        {
            var existing = collection.FirstOrDefault(x => Equals(x.BusinessEntityId, freshItem.BusinessEntityId));
            if (existing != null) existing.CopyScalarsFrom(freshItem);
            else collection.Add(freshItem);
        }
        for (int i = collection.Count - 1; i >= 0; i--)
            if (!freshIds.Contains(collection[i].BusinessEntityId)) collection.RemoveAt(i);

        // Explicitly flag the collection as loaded after a successful database query
        MainModel.Instance.IsBusinessEntitiesLoaded = true;
        OnRefreshed();
    }

    public static void UnloadAll()
    {
        OnUnloading();
        MainModel.Instance.UnloadBusinessEntitiesStorage();
        OnUnloaded();
    }

    static partial void OnRefreshing();
    static partial void OnRefreshed();
    static partial void OnUnloading();
    static partial void OnUnloaded();

    internal void CopyScalarsFrom(BusinessEntity fresh)
    {
        BusinessEntityId = fresh.BusinessEntityId;
        Rowguid = fresh.Rowguid;
        ModifiedDate = fresh.ModifiedDate;
    }

    protected override Task OnSaveAsync(CancellationToken ct) =>
        MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySaver<BusinessEntity>>().SaveAsync(this, ct);
}
}

namespace MILL09.Models
{
public partial class MainModel
{
    private ObservableCollection<BusinessEntity>? _businessEntities;
    public ObservableCollection<BusinessEntity> BusinessEntities
    {
        get { if (_businessEntities == null) BusinessEntities = new ObservableCollection<BusinessEntity>(); return _businessEntities!; }
        private set => SetProperty(ref _businessEntities, value);
    }

    private bool _isBusinessEntitiesLoaded;
    public bool IsBusinessEntitiesLoaded
    {
        get => _isBusinessEntitiesLoaded;
        internal set => SetProperty(ref _isBusinessEntitiesLoaded, value);
    }

    internal void UnloadBusinessEntitiesStorage()
    {
        if (_businessEntities == null) return;
        _businessEntities = null;
        OnPropertyChanged(nameof(BusinessEntities));

        // Reset the load state flag
        IsBusinessEntitiesLoaded = false;
    }
}
}
