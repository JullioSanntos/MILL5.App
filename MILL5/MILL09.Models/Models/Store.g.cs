// <auto-generated>
//     Generated from Store.cs by the ModelsProject template.
//     Regenerated whenever the source file changes.
// </auto-generated>

#nullable enable

#pragma warning disable IDE0130 // Namespace does not match folder structure

using System;
using System.Linq;
using System.Collections.ObjectModel;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.DependencyInjection;
using CommunityToolkit.Mvvm.ComponentModel;
using MILL09.Models.Interfaces;
using MILL80.Infrastructure.Services;
using MILL80.Infrastructure;

namespace MILL09.Models
{
public partial class Store : ModelEntityBase
{
    [ObservableProperty]
    private int _businessEntityId;

    [ObservableProperty]
    private string _name = null!;

    [ObservableProperty]
    private int? _salesPersonId;

    [ObservableProperty]
    private string _demographics = null!;

    [ObservableProperty]
    private Guid _rowguid;

    [ObservableProperty]
    private DateTime _modifiedDate;

    public BusinessEntity? BusinessEntity => MainModel.Instance.BusinessEntities.FirstOrDefault(x => x.BusinessEntityId == this.BusinessEntityId);

    // TODO: could not auto-detect the foreign key for 'SalesPerson' -> SalesPerson.
    // Add a manual implementation in a separate partial file, e.g.:
    // public SalesPerson? SalesPerson => MainModel.Instance.SalesPersons.FirstOrDefault(x => /* match condition */);

    private ObservableCollection<Customer>? _customers;
    public ObservableCollection<Customer> Customers
    {
        // TODO: could not auto-detect the foreign key linking Customer back to Store.
        // Example: new LiveFilteredCollection<Customer>(MainModel.Instance.Customers, x => /* match condition */, nameof(Customer.SomeProperty))
        get { if (_customers == null) Customers = new ObservableCollection<Customer>(); return _customers!; }
        private set => SetProperty(ref _customers, value);
    }

    public static bool IsStoresLoaded => MainModel.Instance.IsStoresLoaded;

    public static Task LoadAllAsync(CancellationToken ct = default) =>
        IsStoresLoaded ? Task.CompletedTask : RefreshAllAsync(ct);

    public static async Task RefreshAllAsync(CancellationToken ct = default)
    {
        OnRefreshing();
        var loader = MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySetLoader<Store>>();
        var fresh = await loader.LoadAllAsync(ct);
        var freshIds = fresh.Select(x => x.BusinessEntityId).ToHashSet();
        var collection = MainModel.Instance.Stores;
        foreach (var freshItem in fresh)
        {
            var existing = collection.FirstOrDefault(x => Equals(x.BusinessEntityId, freshItem.BusinessEntityId));
            if (existing != null) existing.CopyScalarsFrom(freshItem);
            else collection.Add(freshItem);
        }
        for (int i = collection.Count - 1; i >= 0; i--)
            if (!freshIds.Contains(collection[i].BusinessEntityId)) collection.RemoveAt(i);

        // Explicitly flag the collection as loaded after a successful database query
        MainModel.Instance.IsStoresLoaded = true;
        OnRefreshed();
    }

    public static void UnloadAll()
    {
        OnUnloading();
        MainModel.Instance.UnloadStoresStorage();
        OnUnloaded();
    }

    static partial void OnRefreshing();
    static partial void OnRefreshed();
    static partial void OnUnloading();
    static partial void OnUnloaded();

    internal void CopyScalarsFrom(Store fresh)
    {
        BusinessEntityId = fresh.BusinessEntityId;
        Name = fresh.Name;
        SalesPersonId = fresh.SalesPersonId;
        Demographics = fresh.Demographics;
        Rowguid = fresh.Rowguid;
        ModifiedDate = fresh.ModifiedDate;
    }

    protected override Task OnSaveAsync(CancellationToken ct) =>
        MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySaver<Store>>().SaveAsync(this, ct);
}
}

namespace MILL09.Models
{
public partial class MainModel
{
    private ObservableCollection<Store>? _stores;
    public ObservableCollection<Store> Stores
    {
        get { if (_stores == null) Stores = new ObservableCollection<Store>(); return _stores!; }
        private set => SetProperty(ref _stores, value);
    }

    private bool _isStoresLoaded;
    public bool IsStoresLoaded
    {
        get => _isStoresLoaded;
        internal set => SetProperty(ref _isStoresLoaded, value);
    }

    internal void UnloadStoresStorage()
    {
        if (_stores == null) return;
        _stores = null;
        OnPropertyChanged(nameof(Stores));

        // Reset the load state flag
        IsStoresLoaded = false;
    }
}
}
