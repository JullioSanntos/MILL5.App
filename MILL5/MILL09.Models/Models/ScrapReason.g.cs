// <auto-generated>
//     Generated from ScrapReason.cs by the ModelsProject template.
//     Regenerated whenever the source file changes.
// </auto-generated>

#nullable enable

#pragma warning disable IDE0130 // Namespace does not match folder structure

using System;
using System.Linq;
using System.Collections.ObjectModel;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.DependencyInjection;
using CommunityToolkit.Mvvm.ComponentModel;
using MILL09.Models.Interfaces;
using MILL80.Infrastructure.Services;
using MILL80.Infrastructure;

namespace MILL09.Models
{
public partial class ScrapReason : ModelEntityBase
{
    [ObservableProperty]
    private short _scrapReasonId;

    [ObservableProperty]
    private string _name = null!;

    [ObservableProperty]
    private DateTime _modifiedDate;

    private ObservableCollection<WorkOrder>? _workOrders;
    public ObservableCollection<WorkOrder> WorkOrders
    {
        get { if (_workOrders == null) WorkOrders = new LiveFilteredCollection<WorkOrder>(MainModel.Instance.WorkOrders, x => x.ScrapReasonId == this.ScrapReasonId, nameof(WorkOrder.ScrapReasonId)); return _workOrders!; }
        private set => SetProperty(ref _workOrders, value);
    }

    public static bool IsScrapReasonsLoaded => MainModel.Instance.IsScrapReasonsLoaded;

    public static Task LoadAllAsync(CancellationToken ct = default) =>
        IsScrapReasonsLoaded ? Task.CompletedTask : RefreshAllAsync(ct);

    public static async Task RefreshAllAsync(CancellationToken ct = default)
    {
        OnRefreshing();
        var loader = MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySetLoader<ScrapReason>>();
        var fresh = await loader.LoadAllAsync(ct);
        var freshIds = fresh.Select(x => x.ScrapReasonId).ToHashSet();
        var collection = MainModel.Instance.ScrapReasons;
        foreach (var freshItem in fresh)
        {
            var existing = collection.FirstOrDefault(x => Equals(x.ScrapReasonId, freshItem.ScrapReasonId));
            if (existing != null) existing.CopyScalarsFrom(freshItem);
            else collection.Add(freshItem);
        }
        for (int i = collection.Count - 1; i >= 0; i--)
            if (!freshIds.Contains(collection[i].ScrapReasonId)) collection.RemoveAt(i);

        // Explicitly flag the collection as loaded after a successful database query
        MainModel.Instance.IsScrapReasonsLoaded = true;
        OnRefreshed();
    }

    public static void UnloadAll()
    {
        OnUnloading();
        MainModel.Instance.UnloadScrapReasonsStorage();
        OnUnloaded();
    }

    static partial void OnRefreshing();
    static partial void OnRefreshed();
    static partial void OnUnloading();
    static partial void OnUnloaded();

    internal void CopyScalarsFrom(ScrapReason fresh)
    {
        ScrapReasonId = fresh.ScrapReasonId;
        Name = fresh.Name;
        ModifiedDate = fresh.ModifiedDate;
    }

    protected override Task OnSaveAsync(CancellationToken ct) =>
        MILL80.Infrastructure.ServiceLocator.CurrentProvider.GetRequiredService<IEntitySaver<ScrapReason>>().SaveAsync(this, ct);
}
}

namespace MILL09.Models
{
public partial class MainModel
{
    private ObservableCollection<ScrapReason>? _scrapReasons;
    public ObservableCollection<ScrapReason> ScrapReasons
    {
        get { if (_scrapReasons == null) ScrapReasons = new ObservableCollection<ScrapReason>(); return _scrapReasons!; }
        private set => SetProperty(ref _scrapReasons, value);
    }

    private bool _isScrapReasonsLoaded;
    public bool IsScrapReasonsLoaded
    {
        get => _isScrapReasonsLoaded;
        internal set => SetProperty(ref _isScrapReasonsLoaded, value);
    }

    internal void UnloadScrapReasonsStorage()
    {
        if (_scrapReasons == null) return;
        _scrapReasons = null;
        OnPropertyChanged(nameof(ScrapReasons));

        // Reset the load state flag
        IsScrapReasonsLoaded = false;
    }
}
}
